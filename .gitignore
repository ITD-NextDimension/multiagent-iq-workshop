# OneDrive sync artifacts
.!*!*

# Local environment
.env

# macOS
.DS_Store

# 类型检查缓存
.mypy_cache/

# 上游仓库副本（含真实密钥，绝不能进版本库）
docs/*.zip

# Lab 05-1 讲师配置（含真实订阅与资源标识）
code/cloud/scripts/workshop-web.env

# 课件 PPT 一律不进版本库：体积大，且由讲师在本仓库之外维护。
#   - Microsoft Solutions Day PPT template.pptx —— 微软官方模版，17MB，
#     不是本项目的内容，也不该由本仓库转发；需要时从 Microsoft 内部渠道获取。
#   - AKS-MultiAgentIQ_*.pptx —— 教学与介绍课件，见 INSTRUCTOR.md。
docs/*.pptx
