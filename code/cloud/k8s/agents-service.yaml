apiVersion: v1
kind: Service
metadata:
  name: opc-agents
  namespace: opc-iq
  labels:
    app: opc-agents
spec:
  type: LoadBalancer
  selector:
    app: opc-agents
  ports:
    - name: http
      port: 80
      targetPort: 8000
