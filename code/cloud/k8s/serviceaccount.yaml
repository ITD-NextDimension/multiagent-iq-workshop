# Service account federated to the agents' user-assigned managed identity.
# __AGENTS_CLIENT_ID__ is substituted by scripts/deploy.sh.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: opc-agents-sa
  namespace: opc-iq
  annotations:
    azure.workload.identity/client-id: "__AGENTS_CLIENT_ID__"
