# Pinned to the versions validated for this workshop.
# Three pins are load-bearing — relaxing them breaks the labs:
#   * mcp 2.x removed `mcp.server.fastmcp`, which mcp/server.py imports.
#   * The `agent-framework` meta package pulls in ~31 optional integrations and
#     fails pip's resolver; install the four sub-packages below instead.
#   * pydantic-monty >= 0.0.17 changed the API the Monty CodeAct sandbox uses,
#     which stops DataAnalystAgent from producing charts.
agent-framework-core==1.14.0
agent-framework-openai==1.13.0
agent-framework-orchestrations==1.1.0
agent-framework-monty==1.0.0b260730
pydantic-monty==0.0.16
azure-identity==1.25.3
azure-communication-email==1.1.0
python-dotenv==1.2.2
matplotlib==3.11.1
fastapi==0.139.2
uvicorn==0.52.3
mcp==1.29.0
